{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --prefix packages/storage-0g"
  },
  "deploy": {
    "startCommand": "node --experimental-strip-types packages/storage-0g/scripts/live-roundtrip.ts",
    "restartPolicyType": "NEVER"
  }
}
