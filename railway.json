{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --no-frozen-lockfile"
  },
  "deploy": {
    "startCommand": "pnpm m2:live",
    "restartPolicyType": "NEVER"
  }
}
