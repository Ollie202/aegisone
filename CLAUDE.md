# Claude

Read `AGENTS.md`, `PROJECT_STATE.md`, `docs/ARCHITECTURE.md` and `docs/TRUST.md`, then inspect the code you are changing.

This is a solo project. Work directly on `main`; do not create issues, branches, PRs, milestone docs, sprint files or ADRs unless explicitly asked.

Prefer simpler code and fewer abstractions. Preserve the trust/security invariants and run the relevant tests before finishing.
